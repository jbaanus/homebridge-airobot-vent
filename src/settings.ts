/**
 * This is the name of the platform that users will use to register the plugin in the Homebridge config.json
 */
export const PLATFORM_NAME = 'AirobotVentilation';

/**
 * This must match the name of your plugin as defined the package.json `name` property
 */
export const PLUGIN_NAME = 'homebridge-airobot-vent';

/**
 * Manual build counter for runtime verification in Homebridge logs.
 * Bump this value on each commit/release you want to verify.
 */
export const BUILD_COUNTER = 1;
